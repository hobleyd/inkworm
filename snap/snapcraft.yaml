name: inkworm
base: core24
version: 'dev'
summary: A paginated ePub reader
description: |
  Inkworm is a paginated ePub reader with clean typography and smooth
  page navigation.
grade: stable
confinement: classic

apps:
  inkworm:
    command: inkworm

parts:
  inkworm:
    plugin: dump
    source: build/linux/x64/release/bundle
